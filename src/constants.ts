/**
 * Footer panel contribution id declared in the plugin manifesto.
 */
export const MOCK_SERVER_PANEL_ID = "mock-server.panel";

/**
 * Full-page main view contribution id declared in the plugin manifesto.
 */
export const MOCK_SERVER_VIEW_ID = "mock-server.view";

/**
 * Global variable name set when the mock server starts.
 */
export const MOCK_BASE_URL_VARIABLE_NAME = "mockBaseUrl";

/**
 * Template string for the mock base URL global variable.
 */
export const MOCK_BASE_URL_VARIABLE = `{{${MOCK_BASE_URL_VARIABLE_NAME}}}`;
